import Gfunction

def startGame(number):
    guess == result(number)
    print(guess)
    while True:
        user_ques = input('do u want to continue, y/n')
        if user_ques == 'n':
            return 'Thank You'
        else:
            guess = result(guess)
            print(guess)
    print(guess)
